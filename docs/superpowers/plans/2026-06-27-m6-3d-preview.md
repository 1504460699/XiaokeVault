# M6 3D 模型预览 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development or superpowers:executing-plans.

**Goal:** 在预览面板里用三维视口查看 3D 模型（obj/fbx/gltf/glb/dae），支持旋转/缩放/线框。blend 经本机 Blender 转 glb 后预览。

**Architecture:** 前端用 Three.js 渲染。按扩展名选 Loader（GLTFLoader/FBXLoader/ColladaLoader/OBJLoader），通过 convertFileSrc 把本地文件转成 asset URL 加载。blend 文件由后端调用本机 Blender（命令行 --background 导出 glb）转换并缓存。viewer `3d` 路由到 ModelViewer 组件。

**Tech Stack:** three（前端）, Rust std::process（Blender 调用）

**Spec:** `docs/superpowers/specs/2026-06-27-game-asset-manager-design.md` §3.2 model3d/source_blend, §4.4 preview 3d, §5.3 ModelViewer, §6.1 风险

**实测数据：** 13_3D模型分类 obj369/glb192/fbx209/dae250；其他分类 obj173/fbx172/blend108

**前置条件:** M1（model3d/source_blend kind）, M4（viewer 路由）已完成

---

## 关键决策

**Loader 路由（按扩展名）：**
- glb/gltf → GLTFLoader（原生支持，含动画/材质/贴图）
- fbx → FBXLoader（需配套 fbx 依赖）
- dae → ColladaLoader
- obj → OBJLoader（配合同目录 .mtl + 贴图）
- blend → 后端转 glb，前端再走 GLTFLoader

**Three.js 加载本地文件：** convertFileSrc 得到 `http://asset.localhost/...` URL，Three Loader 多数支持 URL 加载。但 OBJ 的 .mtl/贴图是相对路径，asset 协议下相对路径解析可能失败——M6 v1 对 obj 仅加载几何（不含外部材质），复杂材质留后续。

**Blender 转换（blend→glb）：**
- 后端检测本机 Blender 路径（常见位置 + PATH）
- 调 `blender --background input.blend --python export_glb.py`
- 导出的 glb 缓存到 app data，下次直接用
- 缺 Blender 时优雅降级（提示安装，显示文件信息）

---

## 文件结构

```
src/
├── components/preview/
│   └── ModelViewer.vue          # Three.js 三维视口
├── ipc/
│   └── model.ts                 # get_model_glb（blend 转换）
src-tauri/src/
├── preview.rs                   # Blender 调用 + 缓存（新增）
└── lib.rs                       # 注册 command
```

---

## Task 1: 装 three + 创建 ModelViewer 基础视口

**Files:**
- Modify: `package.json`（装 three + @types/three）
- Create: `src/components/preview/ModelViewer.vue`

- [ ] **Step 1: 装 three**

```bash
cd "D:/Xiaoke/Works/Tools/XiaokeTools" && npm install three && npm install -D @types/three
```

- [ ] **Step 2: 创建 src/components/preview/ModelViewer.vue（基础视口 + glb 加载）**

```vue
<script setup lang="ts">
import { ref, onMounted, onUnmounted, watch } from "vue";
import * as THREE from "three";
import { GLTFLoader } from "three/examples/jsm/loaders/GLTFLoader.js";
import { OrbitControls } from "three/examples/jsm/controls/OrbitControls.js";
import { convertFileSrc } from "@tauri-apps/api/core";
import type { FileNode } from "../../types/library";

const props = defineProps<{ file: FileNode }>();
const containerRef = ref<HTMLElement | null>(null);
const status = ref("加载中…");
const wireframe = ref(false);

let renderer: THREE.WebGLRenderer | null = null;
let scene: THREE.Scene | null = null;
let camera: THREE.PerspectiveCamera | null = null;
let controls: OrbitControls | null = null;
let frameId = 0;
let currentMeshes: THREE.Object3D[] = [];

function clearScene() {
  for (const m of currentMeshes) {
    scene?.remove(m);
  }
  currentMeshes = [];
}

function setupScene() {
  const el = containerRef.value!;
  const w = el.clientWidth;
  const h = el.clientHeight;
  scene = new THREE.Scene();
  scene.background = new THREE.Color(0x0f172a);
  camera = new THREE.PerspectiveCamera(50, w / h, 0.01, 1000);
  camera.position.set(3, 2, 5);
  renderer = new THREE.WebGLRenderer({ antialias: true });
  renderer.setSize(w, h);
  renderer.setPixelRatio(window.devicePixelRatio);
  el.appendChild(renderer.domElement);
  controls = new OrbitControls(camera, renderer.domElement);
  controls.enableDamping = true;
  // 灯光
  scene.add(new THREE.HemisphereLight(0xffffff, 0x444444, 1.2));
  const dir = new THREE.DirectionalLight(0xffffff, 1.0);
  dir.position.set(5, 10, 7);
  scene.add(dir);
  animate();
}

function animate() {
  frameId = requestAnimationFrame(animate);
  controls?.update();
  if (renderer && scene && camera) renderer.render(scene, camera);
}

function fitCameraToObject(object: THREE.Object3D) {
  const box = new THREE.Box3().setFromObject(object);
  const size = box.getSize(new THREE.Vector3());
  const center = box.getCenter(new THREE.Vector3());
  const maxDim = Math.max(size.x, size.y, size.z);
  const fov = camera!.fov * (Math.PI / 180);
  let dist = maxDim / 2 / Math.tan(fov / 2);
  dist *= 1.8;
  camera!.position.set(center.x + dist, center.y + dist * 0.7, center.z + dist);
  camera!.lookAt(center);
  controls!.target.copy(center);
  controls!.update();
}

async function loadModel(f: FileNode) {
  clearScene();
  status.value = "加载中…";
  const url = convertFileSrc(f.abs_path);
  const ext = f.ext.toLowerCase();
  try {
    if (ext === "glb" || ext === "gltf") {
      await loadGLTF(url);
    } else {
      status.value = `${ext} 格式暂不支持预览（M6 v1 仅支持 glb/gltf）`;
    }
  } catch (e) {
    status.value = "加载失败：" + String(e);
  }
}

function loadGLTF(url: string): Promise<void> {
  return new Promise((resolve, reject) => {
    const loader = new GLTFLoader();
    loader.load(
      url,
      (gltf) => {
        const obj = gltf.scene;
        scene!.add(obj);
        currentMeshes.push(obj);
        fitCameraToObject(obj);
        status.value = "";
        resolve();
      },
      undefined,
      (err) => reject(err),
    );
  });
}

function toggleWireframe() {
  wireframe.value = !wireframe.value;
  for (const m of currentMeshes) {
    m.traverse((child) => {
      const mesh = child as THREE.Mesh;
      if (mesh.isMesh && mesh.material) {
        const mat = mesh.material as THREE.Material | THREE.Material[];
        if (Array.isArray(mat)) mat.forEach((x) => (x.wireframe = wireframe.value));
        else mat.wireframe = wireframe.value;
      }
    });
  }
}

onMounted(() => {
  setupScene();
  loadModel(props.file);
});

watch(
  () => props.file,
  (f) => loadModel(f),
);

onUnmounted(() => {
  cancelAnimationFrame(frameId);
  renderer?.dispose();
  if (renderer && containerRef.value) {
    containerRef.value.removeChild(renderer.domElement);
  }
});
</script>

<template>
  <div class="w-full h-full flex flex-col">
    <div class="flex items-center gap-2 px-2 py-1 bg-slate-800 border-b border-slate-700 text-xs shrink-0">
      <button class="px-2 py-0.5 rounded bg-slate-600 hover:bg-slate-500" @click="toggleWireframe">
        {{ wireframe ? "实体" : "线框" }}
      </button>
      <span class="text-slate-500">拖拽旋转 · 滚轮缩放</span>
    </div>
    <div ref="containerRef" class="flex-1 relative">
      <div v-if="status" class="absolute inset-0 flex items-center justify-center text-xs text-slate-400 pointer-events-none">
        {{ status }}
      </div>
    </div>
  </div>
</template>
```

- [ ] **Step 3: 验证编译**

Run: `npm run build`
若 `three/examples/jsm/...` 路径报错，调整为 `three/addons/...`。

- [ ] **Step 4: Commit**

```bash
git -C "D:/Xiaoke/Works/Tools/XiaokeTools" add -A
git -C "D:/Xiaoke/Works/Tools/XiaokeTools" commit -m "feat(m6): ModelViewer with Three.js (glb/gltf loading, orbit controls, wireframe)"
```

---

## Task 2: PreviewPane 接入 ModelViewer

**Files:**
- Modify: `src/components/PreviewPane.vue`

- [ ] **Step 1: PreviewPane 的 viewer='3d' 路由到 ModelViewer**

import 加：

```ts
import ModelViewer from "./preview/ModelViewer.vue";
```

预览区分支加（在 SourcePlaceholder 前）：

```html
<ModelViewer v-else-if="viewer === '3d'" :file="file" class="flex-1 min-h-0" />
```

并把 `binary-source` 的 blend 单独提示（blend 不是 3d viewer，走后端转换，M6 v2）。

- [ ] **Step 2: 验证编译**

Run: `npm run build`

- [ ] **Step 3: Commit**

```bash
git -C "D:/Xiaoke/Works/Tools/XiaokeTools" add -A
git -C "D:/Xiaoke/Works/Tools/XiaokeTools" commit -m "feat(m6): route 3d viewer to ModelViewer in PreviewPane"
```

---

## Task 3: 扩展 Loader 支持 obj/fbx/dae

glb/gltf 是最佳格式，但库里 obj/fbx/dae 更多。M6 v1 用 Three 的其他 Loader 支持。

**Files:**
- Modify: `src/components/preview/ModelViewer.vue`

- [ ] **Step 1: import 各 Loader**

```ts
import { OBJLoader } from "three/examples/jsm/loaders/OBJLoader.js";
import { FBXLoader } from "three/examples/jsm/loaders/FBXLoader.js";
import { ColladaLoader } from "three/examples/jsm/loaders/ColladaLoader.js";
```

- [ ] **Step 2: loadModel 扩展分发**

```ts
async function loadModel(f: FileNode) {
  clearScene();
  status.value = "加载中…";
  const url = convertFileSrc(f.abs_path);
  const ext = f.ext.toLowerCase();
  try {
    if (ext === "glb" || ext === "gltf") {
      await loadGLTF(url);
    } else if (ext === "obj") {
      await loadSimple(new OBJLoader(), url);
    } else if (ext === "dae") {
      await loadCollada(url);
    } else if (ext === "fbx") {
      await loadFBX(url);
    } else {
      status.value = `${ext} 暂不支持预览`;
    }
  } catch (e) {
    status.value = "加载失败：" + String(e);
  }
}

function loadSimple(loader: { load: (u: string, ok: (o: THREE.Object3D) => void, p: unknown, err: (e: unknown) => void) => void }, url: string): Promise<void> {
  return new Promise((resolve, reject) => {
    loader.load(url, (obj) => {
      scene!.add(obj);
      currentMeshes.push(obj);
      fitCameraToObject(obj);
      status.value = "";
      resolve();
    }, undefined, reject);
  });
}

function loadCollada(url: string): Promise<void> {
  return new Promise((resolve, reject) => {
    new ColladaLoader().load(url, (res) => {
      const obj = res.scene;
      scene!.add(obj);
      currentMeshes.push(obj);
      fitCameraToObject(obj);
      status.value = "";
      resolve();
    }, undefined, reject);
  });
}

function loadFBX(url: string): Promise<void> {
  return new Promise((resolve, reject) => {
    new FBXLoader().load(url, (obj) => {
      scene!.add(obj);
      currentMeshes.push(obj);
      fitCameraToObject(obj);
      status.value = "";
      resolve();
    }, undefined, reject);
  });
}
```

- [ ] **Step 3: 验证编译**

Run: `npm run build`
FBXLoader 可能需要额外 fflate 依赖：`npm install fflate`（如报错）。

- [ ] **Step 4: Commit**

```bash
git -C "D:/Xiaoke/Works/Tools/XiaokeTools" add -A
git -C "D:/Xiaoke/Works/Tools/XiaokeTools" commit -m "feat(m6): ModelViewer supports obj/fbx/dae loaders"
```

---

## Task 4: blend 转换（后端调用本机 Blender）

**Files:**
- Create: `src-tauri/src/preview.rs`
- Modify: `src-tauri/src/lib.rs`
- Create: `src/ipc/model.ts`
- Modify: `src/components/preview/ModelViewer.vue`（blend 走转换）

- [ ] **Step 1: 创建 src-tauri/src/preview.rs**

```rust
use serde::Serialize;
use std::path::PathBuf;
use std::process::Command;

#[derive(Debug, Serialize)]
pub struct ModelPath {
    pub path: String,    // 可加载的 glb 路径（已转换或缓存）
    pub source: String,  // "direct" / "blender" / "error"
}

/// 检测本机 Blender 路径
fn find_blender() -> Option<PathBuf> {
    // 常见安装位置
    let candidates = [
        r"C:\Program Files\Blender Foundation\Blender\blender.exe",
        r"C:\Program Files\Blender Foundation\Blender 4.2\blender.exe",
        r"C:\Program Files\Blender Foundation\Blender 4.1\blender.exe",
    ];
    for c in candidates {
        if std::path::Path::new(c).exists() {
            return Some(PathBuf::from(c));
        }
    }
    // PATH
    if Command::new("blender").arg("--version").output().is_ok() {
        return Some(PathBuf::from("blender"));
    }
    None
}

/// 返回 blend 转换后的 glb 路径（带缓存）
#[tauri::command]
pub async fn get_model_glb(blend_path: String) -> Result<ModelPath, String> {
    // 缓存目录：<appdata>/com.xiaoke.tauri-app/glb_cache/<hash>.glb
    let mut cache = dirs::data_dir().ok_or("no data dir")?;
    cache.push("com.xiaoke.tauri-app");
    cache.push("glb_cache");
    std::fs::create_dir_all(&cache).map_err(|e| e.to_string())?;
    let stem = std::path::Path::new(&blend_path)
        .file_stem()
        .and_then(|s| s.to_str())
        .unwrap_or("model");
    let glb = cache.join(format!("{}.glb", stem));
    if glb.exists() {
        return Ok(ModelPath { path: glb.to_string_lossy().to_string(), source: "blender".into() });
    }
    // 调 Blender 转换
    let blender = match find_blender() {
        Some(b) => b,
        None => return Ok(ModelPath {
            path: blend_path,
            source: "error".into(),
        }),
    };
    // 写导出脚本
    let script = cache.join("export_glb.py");
    std::fs::write(&script, r#"
import bpy, sys
bpy.ops.wm.open_mainfile(filepath=sys.argv[-2])
for o in bpy.data.objects:
    if o.type == 'MESH':
        o.select_set(True)
out = sys.argv[-1]
bpy.ops.export_scene.gltf(filepath=out, export_format='GLB', use_selection=True)
"#).map_err(|e| e.to_string())?;
    let out = Command::new(&blender)
        .arg("--background")
        .arg(&blend_path)
        .arg("--python")
        .arg(&script)
        .arg("--")
        .arg(&blend_path)
        .arg(&glb)
        .output()
        .map_err(|e| e.to_string())?;
    if !out.status.success() || !glb.exists() {
        return Ok(ModelPath {
            path: blend_path,
            source: "error".into(),
        });
    }
    Ok(ModelPath { path: glb.to_string_lossy().to_string(), source: "blender".into() })
}
```

- [ ] **Step 2: lib.rs 注册 `mod preview;` + command**

```rust
mod preview;
...
preview::get_model_glb,
```

- [ ] **Step 3: 创建 src/ipc/model.ts**

```ts
import { invoke } from "@tauri-apps/api/core";

export interface ModelPath {
  path: string;
  source: "direct" | "blender" | "error";
}

export async function getModelGlb(blendPath: string): Promise<ModelPath> {
  return invoke<ModelPath>("get_model_glb", { blendPath });
}
```

- [ ] **Step 4: ModelViewer 处理 blend**

loadModel 里 blend 分支：

```ts
} else if (ext === "blend") {
  status.value = "正在用 Blender 转换…（首次较慢）";
  const { getModelGlb } = await import("../../ipc/model");
  const res = await getModelGlb(f.abs_path);
  if (res.source === "error") {
    status.value = "转换失败（未安装 Blender 或模型含非网格对象）";
  } else {
    await loadGLTF(convertFileSrc(res.path));
  }
}
```

- [ ] **Step 5: capabilities 不需改（get_model_glb 是自定义 command）**

- [ ] **Step 6: 验证编译**

Run: `cd src-tauri && cargo build` && `npm run build`

- [ ] **Step 7: Commit**

```bash
git -C "D:/Xiaoke/Works/Tools/XiaokeTools" add -A
git -C "D:/Xiaoke/Works/Tools/XiaokeTools" commit -m "feat(m6): blend->glb conversion via local Blender with caching"
```

---

## Task 5: M6 端到端验收

- [ ] **Step 1: 启动应用** `npm run tauri dev`

- [ ] **Step 2: glb 预览**

进 `13_3D模型`，找一个 `.glb` 文件 → 右栏应出现三维视口，模型显示，可拖拽旋转、滚轮缩放。

- [ ] **Step 3: obj/fbx/dae 预览**

分别找 obj/fbx/dae 文件 → 视口加载（fbx/dae 可能较大，稍等）。

- [ ] **Step 4: 线框模式**

点「线框」按钮 → 切换实体/线框。

- [ ] **Step 5: blend 预览（如装了 Blender）**

找 `.blend` 文件 → 提示"正在用 Blender 转换"→ 转 glb 后显示。

- [ ] **Step 6: 验收点**

- [x] glb 在 Three.js 视口可旋转查看
- [x] obj/fbx/dae 可加载
- [x] blend 经本机 Blender 转换后可预览（装了 Blender 时）

- [ ] **Step 7: Commit**

```bash
git -C "D:/Xiaoke/Works/Tools/XiaokeTools" add -A
git -C "D:/Xiaoke/Works/Tools/XiaokeTools" commit -m "test(m6): e2e acceptance - 3d model preview"
```

---

## M6 完成定义

- [ ] Three.js 视口（OrbitControls 旋转/缩放、灯光、自动适配相机）
- [ ] glb/gltf 加载
- [ ] obj/fbx/dae 加载
- [ ] 线框/实体切换
- [ ] blend 经本机 Blender 转 glb（缓存）
- [ ] PreviewPane 的 `3d` viewer 路由到 ModelViewer

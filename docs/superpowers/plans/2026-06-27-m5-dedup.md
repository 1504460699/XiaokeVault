# M5 去重整理 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans.

**Goal:** 检测并清理库里三种重复（压缩包与解压目录并存、疑似备份目录、内容哈希重复），软删除到 trash 可恢复。让库腾出冗余空间。

**Architecture:** 后端新增 `dedup` 模块，扫描时写入 `duplicate_groups` 表。三种检测：zip_extracted（确定性，压缩包+同名解压目录配对）、likely_backup（名字相似度+文件数比对）、hash（疑似重复按需算 sha256，复核）。删除用软删除——移到 app data 的 trash 目录，可恢复。

**Tech Stack:** Rust（sha2, walkdir）, Tauri, Vue 3

**Spec:** `docs/superpowers/specs/2026-06-27-game-asset-manager-design.md` §4.5 dedup, §3.1 duplicate_groups 表

**实测数据：** 13 个 zip + 15 个 `[解压]_` 目录并存，集中在多个包内。

**前置条件:** M1（files/packages 表）已完成

---

## 关键决策

**zip_extracted 配对规则**（基于实测命名）：
- 一个包内：存在 `xxx.zip` 且存在 `[解压]_xxx`（或 `xxx` 已解压的同名目录）→ 标记 zip 为可删
- `x20` 残留处理：zip 名 `Platformer%20Art.zip` → 解压目录 `[解压]_Platformerx20Art`。匹配时把 `%20` 和空格统一规整后比对

**软删除机制**：
- trash 目录：`%APPDATA%\com.xiaoke.tauri-app\trash\<timestamp>\` 保留原相对路径
- 删除前把文件/目录 move 进去（非物理删除），files 表标 `deleted=1`
- 提供"恢复"和"清空 trash"

---

## 文件结构

```
src-tauri/
├── migrations/0003_dedup.sql        # duplicate_groups/members 表
└── src/
    ├── dedup.rs                     # 三种检测 + 软删除
    ├── db.rs                        # 加载 0003 迁移
    └── lib.rs                       # 注册 command
src/
├── ipc/dedup.ts                     # dedup command 封装
├── types/dedup.ts                   # 重复组类型
├── stores/dedupStore.ts             # 检测结果状态
└── components/
    └── DedupPanel.vue               # 去重面板（模态/抽屉）
```

---

## Task 1: duplicate_groups 迁移表

**Files:**
- Create: `src-tauri/migrations/0003_dedup.sql`
- Modify: `src-tauri/src/db.rs`

- [ ] **Step 1: 创建 src-tauri/migrations/0003_dedup.sql**

```sql
CREATE TABLE IF NOT EXISTS duplicate_groups (
    id          INTEGER PRIMARY KEY,
    reason      TEXT NOT NULL,       -- zip_extracted / likely_backup / hash
    detail      TEXT,                -- 人类可读说明
    hash        TEXT,                -- reason=hash 时填
    created_at  INTEGER NOT NULL
);
CREATE TABLE IF NOT EXISTS duplicate_members (
    id          INTEGER PRIMARY KEY,
    group_id    INTEGER NOT NULL REFERENCES duplicate_groups(id) ON DELETE CASCADE,
    file_id     INTEGER REFERENCES files(id) ON DELETE CASCADE,
    package_id  INTEGER REFERENCES packages(id) ON DELETE CASCADE,
    -- member 角色：keep(保留) / remove(建议删除)
    role        TEXT NOT NULL DEFAULT 'remove',
    UNIQUE(group_id, file_id, package_id)
);
CREATE INDEX IF NOT EXISTS idx_dup_members_group ON duplicate_members(group_id);
```

- [ ] **Step 2: db.rs 加载 0003**

在 `migrate` 里追加：

```rust
sqlx::query(include_str!("../migrations/0003_dedup.sql"))
    .execute(pool)
    .await?;
```

- [ ] **Step 3: 验证编译**

Run: `cd src-tauri && cargo build`

- [ ] **Step 4: Commit**

```bash
git -C "D:/Xiaoke/Works/Tools/XiaokeTools" add -A
git -C "D:/Xiaoke/Works/Tools/XiaokeTools" commit -m "feat(m5): duplicate_groups migration"
```

---

## Task 2: dedup 模块 — 三种检测

**Files:**
- Create: `src-tauri/src/dedup.rs`
- Modify: `src-tauri/src/lib.rs`
- Modify: `src-tauri/Cargo.toml`（加 sha2）

- [ ] **Step 1: Cargo.toml 加 sha2**

```toml
sha2 = "0.10"
```

- [ ] **Step 2: 创建 src-tauri/src/dedup.rs**

```rust
use serde::Serialize;
use sha2::{Digest, Sha256};
use sqlx::SqlitePool;
use std::collections::HashMap;
use tauri::State;

#[derive(Debug, Serialize)]
pub struct DupMember {
    pub file_id: Option<i64>,
    pub package_id: Option<i64>,
    pub package_name: Option<String>,
    pub rel_path: Option<String>,
    pub role: String, // keep / remove
}

#[derive(Debug, Serialize)]
pub struct DupGroup {
    pub id: i64,
    pub reason: String,
    pub detail: Option<String>,
    pub members: Vec<DupMember>,
}

#[derive(Debug, Serialize)]
pub struct DedupReport {
    pub groups: i64,
    pub removable_files: i64,
    pub removable_bytes: i64,
}

/// 把 zip 名规整成可匹配的形式：%20 / 空格 / x20 统一
fn norm(s: &str) -> String {
    s.to_lowercase()
        .replace("%20", "")
        .replace(" ", "")
        .replace("x20", "")
        .replace("_", "")
        .replace(".zip", "")
        .replace(".7z", "")
        .replace(".rar", "")
}

/// 检测全部重复，写入 duplicate_groups（先清空旧结果）
#[tauri::command]
pub async fn run_dedup(
    lib_id: i64,
    pool: State<'_, SqlitePool>,
) -> Result<DedupReport, String> {
    let now = chrono::Utc::now().timestamp();
    // 清空旧结果
    sqlx::query("DELETE FROM duplicate_members")
        .execute(&*pool).await.map_err(|e| e.to_string())?;
    sqlx::query("DELETE FROM duplicate_groups")
        .execute(&*pool).await.map_err(|e| e.to_string())?;

    let mut groups = 0i64;
    let mut removable_files = 0i64;
    let mut removable_bytes = 0i64;

    // ---- 1. zip_extracted：包内压缩包 + 解压目录并存 ----
    // 取所有 archive 类文件（含 package 信息）
    let archives: Vec<(i64, String, i64, String)> = sqlx::query_as(
        "SELECT f.id, f.rel_path, p.id, p.name
         FROM files f JOIN packages p ON p.id=f.package_id
         WHERE f.kind='archive' AND f.deleted=0")
        .fetch_all(&*pool).await.map_err(|e| e.to_string())?;
    // 取所有 [解压] 开头目录（从 packages 的子目录扫描，这里简化：查 files 的 rel_path 含 [解压] 的目录）
    // 实际：遍历每个包目录，找 zip 与 [解压]_xxx 的配对
    // 取所有包的磁盘路径
    let pkgs: Vec<(i64, String)> = sqlx::query_as(
        "SELECT id, path FROM packages WHERE category_id IN (SELECT id FROM categories WHERE library_id=?)")
        .bind(lib_id).fetch_all(&*pool).await.map_err(|e| e.to_string())?;

    for (pkg_id, pkg_path) in &pkgs {
        let pkg_dir = std::path::Path::new(pkg_path);
        if !pkg_dir.is_dir() { continue; }
        // 收集该包下的 zip 文件和子目录
        let mut zips: Vec<String> = vec![];
        let mut dirs: Vec<String> = vec![];
        if let Ok(rd) = std::fs::read_dir(pkg_dir) {
            for e in rd.flatten() {
                let name = e.file_name().to_string_lossy().to_string();
                let lower = name.to_lowercase();
                if lower.ends_with(".zip") || lower.ends_with(".7z") || lower.ends_with(".rar") {
                    zips.push(name);
                } else if e.path().is_dir() {
                    dirs.push(name);
                }
            }
        }
        // 配对：zip 名规整后 == 某目录名规整后
        for zip_name in &zips {
            let zn = norm(zip_name);
            for dir_name in &dirs {
                let dn = norm(dir_name);
                if zn == dn || dn.contains(&zn) || zn.contains(&dn) {
                    // 创建一组：zip=remove
                    let (gid,): (i64,) = sqlx::query_as(
                        "INSERT INTO duplicate_groups(reason,detail,created_at) VALUES('zip_extracted',?,?) RETURNING id")
                        .bind(format!("压缩包 {} 已有解压目录 {}", zip_name, dir_name))
                        .bind(now)
                        .fetch_one(&*pool).await.map_err(|e| e.to_string())?;
                    // 找该 zip 对应的 file_id
                    let zip_file: Option<(i64,)> = sqlx::query_as(
                        "SELECT id FROM files WHERE package_id=? AND rel_path=? AND kind='archive'")
                        .bind(pkg_id).bind(zip_name).fetch_optional(&*pool).await
                        .map_err(|e| e.to_string())?;
                    if let Some((fid,)) = zip_file {
                        sqlx::query("INSERT INTO duplicate_members(group_id,file_id,package_id,role) VALUES(?,?,?,'remove')")
                            .bind(gid).bind(fid).bind(pkg_id).execute(&*pool).await
                            .map_err(|e| e.to_string())?;
                        // 算字节数
                        let (bytes,): (i64,) = sqlx::query_as("SELECT bytes FROM files WHERE id=?")
                            .bind(fid).fetch_one(&*pool).await.map_err(|e| e.to_string())?;
                        removable_files += 1;
                        removable_bytes += bytes;
                    }
                    groups += 1;
                    break;
                }
            }
        }
    }

    // likely_backup 与 hash 暂不在 M5 首版实现（M5 聚焦 zip_extracted，覆盖已知主要重复）
    // 标注：M5 v1 仅实现 zip_extracted

    Ok(DedupReport { groups, removable_files, removable_bytes })
}

/// 取所有重复组
#[tauri::command]
pub async fn get_duplicate_groups(pool: State<'_, SqlitePool>) -> Result<Vec<DupGroup>, String> {
    let groups: Vec<(i64, String, Option<String>)> = sqlx::query_as(
        "SELECT id, reason, detail FROM duplicate_groups ORDER BY id")
        .fetch_all(&*pool).await.map_err(|e| e.to_string())?;
    let mut out = Vec::new();
    for (gid, reason, detail) in groups {
        let members: Vec<(Option<i64>, Option<i64>, Option<String>, Option<String>, String)> =
            sqlx::query_as(
                "SELECT dm.file_id, dm.package_id, p.name, f.rel_path, dm.role
                 FROM duplicate_members dm
                 LEFT JOIN files f ON f.id=dm.file_id
                 LEFT JOIN packages p ON p.id=dm.package_id
                 WHERE dm.group_id=?")
            .bind(gid).fetch_all(&*pool).await.map_err(|e| e.to_string())?;
        out.push(DupGroup {
            id: gid, reason, detail,
            members: members.into_iter().map(|(fid,pid,pn,rp,role)| DupMember {
                file_id: fid, package_id: pid, package_name: pn, rel_path: rp, role,
            }).collect(),
        });
    }
    Ok(out)
}

/// 软删除：把 file 物理移到 trash，files 标 deleted=1
#[tauri::command]
pub async fn remove_duplicate(
    file_id: i64,
    pool: State<'_, SqlitePool>,
) -> Result<String, String> {
    let (abs_pkg, rel): (String, String) = sqlx::query_as(
        "SELECT p.path, f.rel_path FROM files f JOIN packages p ON p.id=f.package_id WHERE f.id=?")
        .bind(file_id).fetch_one(&*pool).await.map_err(|e| e.to_string())?;
    let src = std::path::Path::new(&abs_pkg).join(&rel);
    if src.exists() {
        let mut trash = dirs::data_dir().ok_or("no data dir")?;
        trash.push("com.xiaoke.tauri-app");
        trash.push("trash");
        let ts = chrono::Utc::now().timestamp();
        // 保留相对路径结构
        let dest = trash.join(ts.to_string()).join(&abs_pkg.replace(':', "_")).join(&rel);
        if let Some(parent) = dest.parent() {
            std::fs::create_dir_all(parent).map_err(|e| e.to_string())?;
        }
        std::fs::rename(&src, &dest).map_err(|e| e.to_string())?;
        sqlx::query("UPDATE files SET deleted=1 WHERE id=?")
            .bind(file_id).execute(&*pool).await.map_err(|e| e.to_string())?;
        Ok(dest.to_string_lossy().to_string())
    } else {
        // 文件已不在，仅标记
        sqlx::query("UPDATE files SET deleted=1 WHERE id=?")
            .bind(file_id).execute(&*pool).await.map_err(|e| e.to_string())?;
        Ok("文件已不存在，仅标记删除".into())
    }
}

/// 计算文件 sha256（hash 检测备用，M5 v1 暂不主动调用）
#[allow(dead_code)]
fn file_hash(path: &std::path::Path) -> Option<String> {
    let mut hasher = Sha256::new();
    let mut f = std::fs::File::open(path).ok()?;
    std::io::copy(&mut f, &mut hasher).ok()?;
    let _ = HashMap::<(),()>::new(); // 占位避免 unused import 误报
    Some(format!("{:x}", hasher.finalize()))
}
```

- [ ] **Step 3: lib.rs 注册 `mod dedup;` 与 command**

```rust
mod dedup;
...
dedup::run_dedup,
dedup::get_duplicate_groups,
dedup::remove_duplicate,
```

- [ ] **Step 4: 验证编译**

Run: `cd src-tauri && cargo build`
若 `HashMap` 未用导致警告，删除占位行。

- [ ] **Step 5: Commit**

```bash
git -C "D:/Xiaoke/Works/Tools/XiaokeTools" add -A
git -C "D:/Xiaoke/Works/Tools/XiaokeTools" commit -m "feat(m5): dedup module - zip_extracted detection + soft remove to trash"
```

---

## Task 3: 前端类型 + IPC + store

**Files:**
- Create: `src/types/dedup.ts`
- Create: `src/ipc/dedup.ts`
- Create: `src/stores/dedupStore.ts`

- [ ] **Step 1: 创建 src/types/dedup.ts**

```ts
export interface DupMember {
  file_id: number | null;
  package_id: number | null;
  package_name: string | null;
  rel_path: string | null;
  role: "keep" | "remove";
}

export interface DupGroup {
  id: number;
  reason: string;
  detail: string | null;
  members: DupMember[];
}

export interface DedupReport {
  groups: number;
  removable_files: number;
  removable_bytes: number;
}
```

- [ ] **Step 2: 创建 src/ipc/dedup.ts**

```ts
import { invoke } from "@tauri-apps/api/core";
import type { DupGroup, DedupReport } from "../types/dedup";

export const dedupIpc = {
  async runDedup(libId: number): Promise<DedupReport> {
    return invoke<DedupReport>("run_dedup", { libId });
  },
  async getGroups(): Promise<DupGroup[]> {
    return invoke<DupGroup[]>("get_duplicate_groups");
  },
  async removeDuplicate(fileId: number): Promise<string> {
    return invoke<string>("remove_duplicate", { fileId });
  },
};
```

- [ ] **Step 3: 创建 src/stores/dedupStore.ts**

```ts
import { defineStore } from "pinia";
import { ref } from "vue";
import { dedupIpc } from "../ipc/dedup";
import type { DupGroup, DedupReport } from "../types/dedup";

export const useDedupStore = defineStore("dedup", () => {
  const groups = ref<DupGroup[]>([]);
  const report = ref<DedupReport | null>(null);
  const scanning = ref(false);

  async function runDedup(libId: number) {
    scanning.value = true;
    try {
      report.value = await dedupIpc.runDedup(libId);
      groups.value = await dedupIpc.getGroups();
    } finally {
      scanning.value = false;
    }
  }

  async function loadGroups() {
    groups.value = await dedupIpc.getGroups();
  }

  async function removeMember(fileId: number, groupId: number) {
    await dedupIpc.removeDuplicate(fileId);
    // 从列表移除该组
    groups.value = groups.value.filter((g) => g.id !== groupId);
  }

  return { groups, report, scanning, runDedup, loadGroups, removeMember };
});
```

- [ ] **Step 4: 验证编译**

Run: `npm run build`

- [ ] **Step 5: Commit**

```bash
git -C "D:/Xiaoke/Works/Tools/XiaokeTools" add -A
git -C "D:/Xiaoke/Works/Tools/XiaokeTools" commit -m "feat(m5): frontend types/ipc/store for dedup"
```

---

## Task 4: DedupPanel 组件

去重面板：显示重复组列表，每组可"删除冗余"。

**Files:**
- Create: `src/components/DedupPanel.vue`
- Modify: `src/App.vue`（接入面板入口）
- Modify: `src/components/TopBar.vue`（加"去重"按钮）

- [ ] **Step 1: 创建 src/components/DedupPanel.vue**

```vue
<script setup lang="ts">
import { storeToRefs } from "pinia";
import { useDedupStore } from "../stores/dedupStore";
import { useLibraryStore } from "../stores/libraryStore";

const props = defineProps<{ show: boolean }>();
const emit = defineEmits<{ close: [] }>();

const dedup = useDedupStore();
const lib = useLibraryStore();
const { groups, report, scanning } = storeToRefs(dedup);

async function onScan() {
  if (lib.currentLibId === null) return;
  await dedup.runDedup(lib.currentLibId);
}

async function onRemove(groupId: number, fileId: number | null) {
  if (fileId === null) return;
  if (!confirm("确认删除？文件会移到 trash 目录，可恢复。")) return;
  await dedup.removeMember(fileId, groupId);
}

function fmtBytes(b: number): string {
  if (b > 1e9) return (b / 1e9).toFixed(2) + " GB";
  if (b > 1e6) return (b / 1e6).toFixed(1) + " MB";
  if (b > 1e3) return (b / 1e3).toFixed(0) + " KB";
  return b + " B";
}
</script>

<template>
  <Teleport to="body">
    <div v-if="props.show" class="fixed inset-0 bg-black/60 flex items-center justify-center z-50" @click.self="emit('close')">
      <div class="bg-slate-800 rounded-lg p-6 w-[640px] max-h-[80vh] flex flex-col text-slate-100">
        <div class="flex items-center mb-3">
          <h2 class="text-lg font-bold flex-1">去重整理</h2>
          <button class="px-3 py-1 rounded bg-sky-600 hover:bg-sky-500 text-sm disabled:opacity-50"
            :disabled="scanning || lib.currentLibId === null" @click="onScan">
            {{ scanning ? "检测中…" : (groups.length ? "重新检测" : "开始检测") }}
          </button>
        </div>

        <div v-if="report" class="text-xs text-slate-400 mb-3">
          发现 {{ report.groups }} 组重复 · 可清理 {{ report.removable_files }} 文件 · {{ fmtBytes(report.removable_bytes) }}
        </div>

        <div class="flex-1 overflow-auto space-y-2">
          <div v-for="g in groups" :key="g.id" class="bg-slate-900 rounded p-3 border border-slate-700">
            <div class="text-sm text-amber-400 mb-1">⚠ {{ g.reason }}</div>
            <div class="text-xs text-slate-300 mb-2">{{ g.detail }}</div>
            <div v-for="m in g.members" :key="m.file_id" class="text-xs text-slate-400 flex items-center gap-2">
              <span class="flex-1 truncate">{{ m.package_name }} / {{ m.rel_path }}</span>
              <button v-if="m.role === 'remove' && m.file_id" class="px-2 py-0.5 rounded bg-red-700 hover:bg-red-600 text-xs"
                @click="onRemove(g.id, m.file_id)">删除</button>
            </div>
          </div>
          <div v-if="groups.length === 0 && report" class="text-center text-slate-500 py-8">
            ✓ 未发现重复
          </div>
          <div v-if="!report" class="text-center text-slate-500 py-8">
            点击「开始检测」扫描重复
          </div>
        </div>

        <div class="flex justify-end mt-3">
          <button class="px-4 py-1 rounded bg-slate-600 hover:bg-slate-500 text-sm" @click="emit('close')">关闭</button>
        </div>
      </div>
    </div>
  </Teleport>
</template>
```

- [ ] **Step 2: App.vue 接入 DedupPanel**

`<script setup>` 加：

```ts
import DedupPanel from "./components/DedupPanel.vue";
const showDedup = ref(false);
```

模板末尾加（在 ExportDialog 后）：

```html
<DedupPanel :show="showDedup" @close="showDedup = false" />
```

> 注：去重入口按钮放 TopBar（Step 3）。

- [ ] **Step 3: TopBar 加「去重」按钮**

TopBar.vue `<script>` 加：

```ts
defineEmits<{ dedup: [] }>();
```

模板在「扫描」按钮旁加：

```html
<button class="px-3 py-1 rounded bg-slate-700 hover:bg-slate-600 text-sm"
  @click="$emit('dedup')">去重</button>
```

App.vue 的 TopBar 监听：`<TopBar @dedup="showDedup = true" />`

- [ ] **Step 4: 验证编译**

Run: `npm run build`

- [ ] **Step 5: Commit**

```bash
git -C "D:/Xiaoke/Works/Tools/XiaokeTools" add -A
git -C "D:/Xiaoke/Works/Tools/XiaokeTools" commit -m "feat(m5): DedupPanel with detection + soft remove UI"
```

---

## Task 5: M5 端到端验收

- [ ] **Step 1: 启动应用** `npm run tauri dev`

- [ ] **Step 2: 检测重复**

1. 点顶栏「去重」按钮 → 弹出去重面板
2. 点「开始检测」
3. 预期：报告显示约 13 组重复（zip_extracted），可清理 13 文件

- [ ] **Step 3: 删除一个冗余 zip**

1. 在某组点「删除」→ 确认
2. 检查：该 zip 从磁盘移除（移到 trash），列表移除该组
3. 回缩略图墙确认：该包的 zip 文件不再显示（已软删除）

- [ ] **Step 4: 验收点**

- [x] 检测出 zip+解压目录并存
- [x] 软删除后文件移入 trash 可恢复（检查 `%APPDATA%\com.xiaoke.tauri-app\trash\`）

- [ ] **Step 5: Commit**

```bash
git -C "D:/Xiaoke/Works/Tools/XiaokeTools" add -A
git -C "D:/Xiaoke/Works/Tools/XiaokeTools" commit -m "test(m5): e2e acceptance - dedup detection and soft remove"
```

---

## M5 完成定义（v1）

- [ ] duplicate_groups/members 表
- [ ] zip_extracted 检测（压缩包+解压目录配对）
- [ ] 软删除到 trash 可恢复
- [ ] DedupPanel UI（检测 + 逐组删除）
- [ ] likely_backup / hash 检测留作后续（M5 v1 聚焦已知的 zip 冗余）

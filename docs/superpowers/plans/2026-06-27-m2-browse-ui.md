# M2 浏览基础 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把空的欢迎页改造成三栏浏览界面——左栏分类树、中栏包列表+文件缩略图墙、右栏预览面板。能添加库、扫描、浏览，并显示图片静态缩略图。

**Architecture:** 前端 Vue 3 + Pinia（三个 store）+ Tailwind v4（vite 插件）。统一 IPC 封装层调用 M1 已就绪的 Rust command。缩略图先用 Rust 后端按需生成（M2 只做静态图 image 类）。应用主窗口扩到 1280×800 容纳三栏。

**Tech Stack:** Vue 3, Pinia, Tailwind CSS v4, @tanstack/vue-virtual, @tauri-apps/api

**Spec:** `docs/superpowers/specs/2026-06-27-game-asset-manager-design.md` §4.4 preview(仅 image viewer), §5.1 三栏布局, §5.3 预览组件(仅 ThumbImage), §5.6 状态管理

**前置条件:** M1 已完成（add_library / scan_library_full / get_categories / get_packages / get_package_files command 可用，已装 pinia/tailwind/vue-virtual）

---

## 文件结构

```
src/
├── main.ts                  # 注册 Pinia
├── App.vue                  # 三栏根布局（替换欢迎页）
├── assets/
│   └── main.css             # Tailwind 入口
├── ipc/
│   └── library.ts           # 统一 invoke 封装（带类型与错误处理）
├── stores/
│   ├── libraryStore.ts      # 库/分类/包/文件数据
│   ├── selectionStore.ts    # M2 暂留骨架，M3 填充勾选逻辑
│   └── exportStore.ts       # M2 暂留骨架，M3 填充
├── components/
│   ├── TopBar.vue           # 顶栏：库切换/搜索/扫描按钮
│   ├── CategoryTree.vue     # 左栏：分类列表
│   ├── PackageGrid.vue      # 中栏：包网格（点进去看文件）
│   ├── FileGrid.vue         # 中栏：文件缩略图墙（虚拟滚动）
│   └── PreviewPane.vue      # 右栏：选中文件大图预览
└── types/
    └── library.ts           # 前端类型定义（与 Rust 返回对齐）
```

---

## Task 1: 接入 Tailwind v4 + Pinia

**Files:**
- Modify: `vite.config.ts`
- Create: `src/assets/main.css`
- Modify: `src/main.ts`
- Modify: `src/App.vue`（临时占位，验证 Tailwind 生效）

- [ ] **Step 1: vite.config.ts 加 tailwind 插件**

```ts
import { defineConfig } from "vite";
import vue from "@vitejs/plugin-vue";
import tailwindcss from "@tailwindcss/vite";

const host = process.env.TAURI_DEV_HOST;

export default defineConfig(async () => ({
  plugins: [vue(), tailwindcss()],
  clearScreen: false,
  server: {
    port: 1420,
    strictPort: true,
    host: host || false,
    hmr: host ? { protocol: "ws", host, port: 1421 } : undefined,
    watch: { ignored: ["**/src-tauri/**"] },
  },
}));
```

- [ ] **Step 2: 创建 src/assets/main.css（Tailwind v4 入口）**

```css
@import "tailwindcss";

/* 桌面应用基础：禁用文本选择(除输入框)、深色背景 */
html, body, #app {
  height: 100%;
  margin: 0;
}
body {
  user-select: none;
  -webkit-user-select: none;
}
input, textarea {
  user-select: text;
}
```

- [ ] **Step 3: 更新 src/main.ts 注册 Pinia + 引入 css**

```ts
import { createApp } from "vue";
import { createPinia } from "pinia";
import "./assets/main.css";
import App from "./App.vue";

createApp(App).use(createPinia()).mount("#app");
```

- [ ] **Step 4: App.vue 临时占位验证 Tailwind 生效**

```vue
<script setup lang="ts">
</script>

<template>
  <div class="h-full flex items-center justify-center bg-slate-900 text-slate-100">
    <h1 class="text-2xl font-bold text-sky-400">XiaokeTools 资源管理器</h1>
  </div>
</template>
```

- [ ] **Step 5: 验证编译与 Tailwind 生效**

Run: `npm run build`
Expected: vue-tsc + vite build 通过，无报错。

Run: `npm run tauri dev`（可手动在窗口确认深色背景 + 天蓝色标题渲染）

- [ ] **Step 6: Commit**

```bash
git -C "D:/Xiaoke/Works/Tools/XiaokeTools" add -A
git -C "D:/Xiaoke/Works/Tools/XiaokeTools" commit -m "feat(m2): wire up tailwind v4 and pinia"
```

---

## Task 2: 前端类型定义 + IPC 封装层

与 M1 的 Rust 返回结构对齐（library.rs 的 Serialize 结构）。

**Files:**
- Create: `src/types/library.ts`
- Create: `src/ipc/library.ts`

- [ ] **Step 1: 创建 src/types/library.ts**

```ts
// 与 src-tauri/src/library.rs 的 Serialize 结构对齐
export interface Library {
  id: number;
  name: string;
  root_path: string;
}

export interface Category {
  id: number;
  name: string;
  sort_order: number;
  package_count: number;
  file_count: number;
  total_bytes: number;
}

export interface PackageSummary {
  id: number;
  name: string;
  path: string;
  file_count: number;
  total_bytes: number;
  has_zip: boolean;
  license: string | null;
}

export interface FileNode {
  id: number;
  rel_path: string;
  name: string;
  ext: string;
  kind: string;
  bytes: number;
}

export interface ScanReport {
  new: number;
  updated: number;
  deleted: number;
  total_files: number;
  duration_ms: number;
  errors: string[];
  unknown_extensions: [string, number][];
}

// Rust 的 asset_types::AssetType
export interface AssetType {
  kind: string;
  label: string;
  extensions: string[];
  viewer: string;
  icon: string | null;
  is_source: boolean;
}
```

- [ ] **Step 2: 创建 src/ipc/library.ts（统一 invoke 封装）**

```ts
import { invoke } from "@tauri-apps/api/core";
import type {
  Library, Category, PackageSummary, FileNode, ScanReport,
} from "../types/library";

// 命令名与 src-tauri/src/lib.rs generate_handler 注册一致
export const ipc = {
  async listLibraries(): Promise<Library[]> {
    return invoke<Library[]>("list_libraries");
  },
  async addLibrary(name: string, rootPath: string): Promise<Library> {
    return invoke<Library>("add_library", { name, rootPath });
  },
  async scanLibraryFull(libId: number): Promise<ScanReport> {
    return invoke<ScanReport>("scan_library_full", { libId });
  },
  async getCategories(libId: number): Promise<Category[]> {
    return invoke<Category[]>("get_categories", { libId });
  },
  async getPackages(categoryId: number): Promise<PackageSummary[]> {
    return invoke<PackageSummary[]>("get_packages", { categoryId });
  },
  async getPackageFiles(pkgId: number): Promise<FileNode[]> {
    return invoke<FileNode[]>("get_package_files", { pkgId });
  },
};
```

> **注意 invoke 参数命名**：Tauri 把 JS 的 camelCase 参数转成 Rust 的 snake_case。`libId` → `lib_id`，`categoryId` → `category_id`，`pkgId` → `pkg_id`。M1 的 Rust command 签名用的就是这些 snake_case 名，匹配。

- [ ] **Step 3: 验证编译**

Run: `npm run build`
Expected: 通过。

- [ ] **Step 4: Commit**

```bash
git -C "D:/Xiaoke/Works/Tools/XiaokeTools" add -A
git -C "D:/Xiaoke/Works/Tools/XiaokeTools" commit -m "feat(m2): types and ipc wrapper for library commands"
```

---

## Task 3: libraryStore — 库/分类/包/文件状态

**Files:**
- Create: `src/stores/libraryStore.ts`
- Create: `src/stores/selectionStore.ts`（骨架）
- Create: `src/stores/exportStore.ts`（骨架）

- [ ] **Step 1: 创建 src/stores/libraryStore.ts**

```ts
import { defineStore } from "pinia";
import { ref, computed } from "vue";
import { ipc } from "../ipc/library";
import type { Library, Category, PackageSummary, FileNode, ScanReport } from "../types/library";

export const useLibraryStore = defineStore("library", () => {
  const libraries = ref<Library[]>([]);
  const currentLibId = ref<number | null>(null);
  const categories = ref<Category[]>([]);
  const currentCategoryId = ref<number | null>(null);
  const packages = ref<PackageSummary[]>([]);
  const currentPkgId = ref<number | null>(null);
  const files = ref<FileNode[]>([]);
  const scanning = ref(false);
  const scanReport = ref<ScanReport | null>(null);
  const error = ref<string | null>(null);

  const currentCategory = computed(() =>
    categories.value.find((c) => c.id === currentCategoryId.value) ?? null
  );
  const currentPackage = computed(() =>
    packages.value.find((p) => p.id === currentPkgId.value) ?? null
  );

  async function loadLibraries() {
    libraries.value = await ipc.listLibraries();
    if (currentLibId.value === null && libraries.value.length > 0) {
      currentLibId.value = libraries.value[0].id;
    }
  }

  async function addLibrary(name: string, rootPath: string) {
    const lib = await ipc.addLibrary(name, rootPath);
    libraries.value.push(lib);
    currentLibId.value = lib.id;
  }

  async function selectLibrary(libId: number) {
    currentLibId.value = libId;
    currentCategoryId.value = null;
    currentPkgId.value = null;
    packages.value = [];
    files.value = [];
    await loadCategories();
  }

  async function loadCategories() {
    if (currentLibId.value === null) return;
    categories.value = await ipc.getCategories(currentLibId.value);
    if (currentCategoryId.value === null && categories.value.length > 0) {
      currentCategoryId.value = categories.value[0].id;
    }
  }

  async function selectCategory(catId: number) {
    currentCategoryId.value = catId;
    currentPkgId.value = null;
    files.value = [];
    await loadPackages();
  }

  async function loadPackages() {
    if (currentCategoryId.value === null) return;
    packages.value = await ipc.getPackages(currentCategoryId.value);
  }

  async function selectPackage(pkgId: number) {
    currentPkgId.value = pkgId;
    files.value = await ipc.getPackageFiles(pkgId);
  }

  async function backToPackages() {
    currentPkgId.value = null;
    files.value = [];
  }

  async function scanCurrent() {
    if (currentLibId.value === null) return;
    scanning.value = true;
    error.value = null;
    try {
      scanReport.value = await ipc.scanLibraryFull(currentLibId.value);
      await loadCategories();
      if (currentCategoryId.value !== null) await loadPackages();
    } catch (e: any) {
      error.value = String(e);
    } finally {
      scanning.value = false;
    }
  }

  return {
    libraries, currentLibId, categories, currentCategoryId, currentCategory,
    packages, currentPkgId, currentPackage, files, scanning, scanReport, error,
    loadLibraries, addLibrary, selectLibrary, loadCategories,
    selectCategory, loadPackages, selectPackage, backToPackages, scanCurrent,
  };
});
```

- [ ] **Step 2: 创建 src/stores/selectionStore.ts（M2 骨架，M3 填充）**

```ts
import { defineStore } from "pinia";
import { ref } from "vue";

// M2 仅占位：选中预览的文件。M3 扩展为导出勾选状态。
export const useSelectionStore = defineStore("selection", () => {
  const previewFileId = ref<number | null>(null);
  function setPreview(id: number | null) {
    previewFileId.value = id;
  }
  return { previewFileId, setPreview };
});
```

- [ ] **Step 3: 创建 src/stores/exportStore.ts（M2 骨架，M3 填充）**

```ts
import { defineStore } from "pinia";
import { ref } from "vue";

// M2 占位。M3 填充项目/勾选/导出任务。
export const useExportStore = defineStore("export", () => {
  const ready = ref(false);
  return { ready };
});
```

- [ ] **Step 4: 验证编译**

Run: `npm run build`
Expected: 通过。

- [ ] **Step 5: Commit**

```bash
git -C "D:/Xiaoke/Works/Tools/XiaokeTools" add -A
git -C "D:/Xiaoke/Works/Tools/XiaokeTools" commit -m "feat(m2): libraryStore with scan/browse state machine"
```

---

## Task 4: TopBar + 首次添加库

顶栏含库切换下拉、扫描按钮、扫描报告提示。M2 暂不含搜索（M3+）。

**Files:**
- Create: `src/components/TopBar.vue`
- Modify: `src/App.vue`（组装三栏框架，先接 TopBar）

- [ ] **Step 1: 创建 src/components/TopBar.vue**

```vue
<script setup lang="ts">
import { storeToRefs } from "pinia";
import { useLibraryStore } from "../stores/libraryStore";
import { ipc } from "../ipc/library";

const store = useLibraryStore();
const { libraries, currentLibId, scanning, scanReport, error } = storeToRefs(store);

async function onScan() {
  await store.scanCurrent();
}

async function onAddLibrary() {
  const rootPath = window.prompt("输入素材库根目录（如 D:\\Xiaoke\\GameAssets）：");
  if (!rootPath) return;
  const name = window.prompt("输入库名称：", "GameAssets");
  if (!name) return;
  try {
    await store.addLibrary(name, rootPath);
    await store.loadCategories();
  } catch (e: any) {
    alert("添加失败：" + String(e));
  }
}

function onLibChange(e: Event) {
  const id = Number((e.target as HTMLSelectElement).value);
  store.selectLibrary(id);
}

void ipc; // 占位，避免未用警告
</script>

<template>
  <header class="flex items-center gap-3 px-4 h-12 bg-slate-800 border-b border-slate-700 shrink-0">
    <span class="font-bold text-sky-400">XiaokeTools</span>
    <select
      class="bg-slate-700 text-slate-100 px-2 py-1 rounded text-sm"
      :value="currentLibId ?? ''"
      @change="onLibChange"
    >
      <option value="" disabled>选择库…</option>
      <option v-for="lib in libraries" :key="lib.id" :value="lib.id">{{ lib.name }}</option>
    </select>
    <button
      class="px-3 py-1 rounded bg-sky-600 hover:bg-sky-500 disabled:opacity-50 text-sm"
      :disabled="scanning || currentLibId === null"
      @click="onScan"
    >
      {{ scanning ? "扫描中…" : "扫描" }}
    </button>
    <button
      class="px-3 py-1 rounded bg-slate-700 hover:bg-slate-600 text-sm"
      @click="onAddLibrary"
    >
      + 添加库
    </button>
    <div class="ml-auto text-xs text-slate-400">
      <span v-if="scanReport">上次扫描：{{ scanReport.total_files }} 文件 / {{ Math.round(scanReport.duration_ms / 1000) }}s</span>
      <span v-if="error" class="text-red-400">⚠ {{ error }}</span>
    </div>
  </header>
</template>
```

- [ ] **Step 2: 更新 src/App.vue 组装三栏框架（中栏暂占位）**

```vue
<script setup lang="ts">
import { onMounted } from "vue";
import TopBar from "./components/TopBar.vue";
import CategoryTree from "./components/CategoryTree.vue";
import PackageGrid from "./components/PackageGrid.vue";
import { useLibraryStore } from "./stores/libraryStore";

const store = useLibraryStore();
onMounted(async () => {
  await store.loadLibraries();
  if (store.currentLibId !== null) await store.loadCategories();
});
</script>

<template>
  <div class="h-full flex flex-col bg-slate-900 text-slate-100">
    <TopBar />
    <div class="flex-1 flex overflow-hidden">
      <CategoryTree />
      <PackageGrid />
    </div>
  </div>
</template>
```

> 注：CategoryTree / PackageGrid 在后续 Task 创建。本步先写好引用，编译会失败属正常，Task 5/6 创建后即可。

- [ ] **Step 3: Commit（暂跳过编译验证，等组件补齐）**

```bash
git -C "D:/Xiaoke/Works/Tools/XiaokeTools" add -A
git -C "D:/Xiaoke/Works/Tools/XiaokeTools" commit -m "feat(m2): TopBar with library switch and scan"
```

---

## Task 5: CategoryTree 左栏

**Files:**
- Create: `src/components/CategoryTree.vue`

- [ ] **Step 1: 创建 src/components/CategoryTree.vue**

```vue
<script setup lang="ts">
import { storeToRefs } from "pinia";
import { useLibraryStore } from "../stores/libraryStore";

const store = useLibraryStore();
const { categories, currentCategoryId } = storeToRefs(store);

function fmtBytes(b: number): string {
  if (b > 1e9) return (b / 1e9).toFixed(1) + " GB";
  if (b > 1e6) return (b / 1e6).toFixed(1) + " MB";
  if (b > 1e3) return (b / 1e3).toFixed(0) + " KB";
  return b + " B";
}
</script>

<template>
  <aside class="w-64 shrink-0 overflow-y-auto bg-slate-850 bg-slate-800/50 border-r border-slate-700">
    <ul class="py-2">
      <li
        v-for="cat in categories"
        :key="cat.id"
        class="px-3 py-2 cursor-pointer hover:bg-slate-700/50"
        :class="cat.id === currentCategoryId ? 'bg-sky-600/30 border-l-2 border-sky-400' : ''"
        @click="store.selectCategory(cat.id)"
      >
        <div class="text-sm truncate">{{ cat.name }}</div>
        <div class="text-xs text-slate-400">
          {{ cat.package_count }} 包 · {{ cat.file_count }} 文件 · {{ fmtBytes(cat.total_bytes) }}
        </div>
      </li>
      <li v-if="categories.length === 0" class="px-3 py-4 text-sm text-slate-500">
        无分类。请先扫描库。
      </li>
    </ul>
  </aside>
</template>
```

- [ ] **Step 2: Commit**

```bash
git -C "D:/Xiaoke/Works/Tools/XiaokeTools" add -A
git -C "D:/Xiaoke/Works/Tools/XiaokeTools" commit -m "feat(m2): CategoryTree left pane"
```

---

## Task 6: PackageGrid + FileGrid 中栏

中栏分两级：默认显示当前分类的包网格，点进包显示文件缩略图墙。

**Files:**
- Create: `src/components/PackageGrid.vue`
- Create: `src/components/FileGrid.vue`

- [ ] **Step 1: 创建 src/components/PackageGrid.vue（含包视图 + 进包切换）**

```vue
<script setup lang="ts">
import { storeToRefs } from "pinia";
import { useLibraryStore } from "../stores/libraryStore";
import FileGrid from "./FileGrid.vue";

const store = useLibraryStore();
const { currentPkgId, packages, currentPackage } = storeToRefs(store);

function fmtBytes(b: number): string {
  if (b > 1e9) return (b / 1e9).toFixed(1) + " GB";
  if (b > 1e6) return (b / 1e6).toFixed(1) + " MB";
  if (b > 1e3) return (b / 1e3).toFixed(0) + " KB";
  return b + " B";
}
</script>

<template>
  <main class="flex-1 overflow-hidden flex flex-col">
    <!-- 包内：文件视图 -->
    <FileGrid v-if="currentPkgId !== null" />
    <!-- 包列表视图 -->
    <template v-else>
      <div class="px-4 py-2 text-sm text-slate-400 border-b border-slate-700 shrink-0">
        {{ packages.length }} 个素材包
      </div>
      <div class="flex-1 overflow-y-auto p-4 grid gap-3 content-start" style="grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));">
        <div
          v-for="pkg in packages"
          :key="pkg.id"
          class="bg-slate-800 rounded-lg p-3 cursor-pointer hover:bg-slate-700 border border-slate-700"
          @click="store.selectPackage(pkg.id)"
        >
          <div class="font-medium text-sm truncate">{{ pkg.name }}</div>
          <div class="text-xs text-slate-400 mt-1">
            {{ pkg.file_count }} 文件 · {{ fmtBytes(pkg.total_bytes) }}
          </div>
          <div v-if="pkg.has_zip" class="text-xs text-amber-400 mt-1">⚠ 含压缩包</div>
          <div v-if="pkg.license" class="text-xs text-emerald-400 mt-1">{{ pkg.license }}</div>
        </div>
        <div v-if="packages.length === 0" class="col-span-full text-center text-slate-500 py-8">
          选择左侧分类查看素材包
        </div>
      </div>
    </template>
  </main>
</template>
```

- [ ] **Step 2: 创建 src/components/FileGrid.vue（虚拟滚动缩略图墙）**

```vue
<script setup lang="ts">
import { computed, ref } from "vue";
import { storeToRefs } from "pinia";
import { useVirtualizer } from "@tanstack/vue-virtual";
import { useLibraryStore } from "../stores/libraryStore";
import { useSelectionStore } from "../stores/selectionStore";
import { getFileUrl } from "../ipc/fileUrl";

const store = useLibraryStore();
const sel = useSelectionStore();
const { files, currentPackage } = storeToRefs(store);

const COLS = 6;            // 每行列数
const ROW_H = 150;         // 行高 px
const GAP = 12;

const rows = computed(() => {
  const n = files.value.length;
  const r: FileNode[][] = [];
  for (let i = 0; i < n; i += COLS) r.push(files.value.slice(i, i + COLS));
  return r;
});

const parentRef = ref<HTMLElement | null>(null);
const virtualizer = useVirtualizer({
  count: rows.value.length,
  getScrollElement: () => parentRef.value,
  estimateSize: () => ROW_H + GAP,
  overscan: 4,
});

// 文件缩略图来源：image 类用 convertFileSrc 直接加载；其他用图标
import type { FileNode } from "../types/library";
function thumbFor(f: FileNode): string | null {
  if (f.kind === "image") return getFileUrl(f); // 实现在 ipc/fileUrl.ts（Task 7）
  return null;
}
function isImage(f: FileNode): boolean { return f.kind === "image"; }
</script>

<template>
  <div class="flex-1 flex flex-col overflow-hidden">
    <div class="px-4 py-2 text-sm text-slate-400 border-b border-slate-700 shrink-0 flex items-center gap-2">
      <button class="text-sky-400 hover:underline" @click="store.backToPackages()">← 返回包列表</button>
      <span>/ {{ currentPackage?.name }}</span>
    </div>
    <div ref="parentRef" class="flex-1 overflow-auto p-3">
      <div :style="{ height: `${virtualizer.getTotalSize()}px`, position: 'relative' }">
        <div
          v-for="row in virtualizer.getVirtualItems()"
          :key="row.index"
          :style="{
            position: 'absolute',
            top: 0,
            left: 0,
            transform: `translateY(${row.start}px)`,
            width: '100%',
            height: `${ROW_H}px`,
            display: 'grid',
            gridTemplateColumns: `repeat(${COLS}, 1fr)`,
            gap: `${GAP}px`,
          }"
        >
          <div
            v-for="f in rows[row.index]"
            :key="f.id"
            class="bg-slate-800 rounded border border-slate-700 flex flex-col overflow-hidden cursor-pointer hover:border-sky-500"
            :class="f.id === sel.previewFileId ? 'border-sky-400 ring-1 ring-sky-400' : ''"
            @click="sel.setPreview(f.id)"
          >
            <div class="flex-1 flex items-center justify-center bg-slate-900 overflow-hidden">
              <img v-if="isImage(f)" :src="thumbFor(f)!" class="max-w-full max-h-full object-contain" loading="lazy" />
              <div v-else class="text-3xl">📦</div>
            </div>
            <div class="text-xs text-slate-400 truncate px-1 py-0.5">{{ f.name }}</div>
          </div>
        </div>
      </div>
    </div>
  </div>
</template>
```

> **说明**：FileGrid 引用了 `ipc/fileUrl`（Task 7 创建）。本 Task 先不验证编译。

- [ ] **Step 3: Commit**

```bash
git -C "D:/Xiaoke/Works/Tools/XiaokeTools" add -A
git -C "D:/Xiaoke/Works/Tools/XiaokeTools" commit -m "feat(m2): PackageGrid and virtualized FileGrid"
```

---

## Task 7: 图片缩略图加载（convertFileSrc）

M2 缩略图策略：image 类直接用 Tauri 的 `convertFileSrc` 把本地文件转成 webview 可加载的 URL，**不生成独立缩略图文件**（M4 再做 Rust 侧缩略图生成）。这最简单且即时。

**Files:**
- Create: `src/ipc/fileUrl.ts`
- Modify: `src-tauri/capabilities/default.json`（放开 fs 读权限）
- Modify: `src-tauri/src/library.rs`（给 FileNode 加绝对路径字段）

- [ ] **Step 1: 修改 src-tauri/src/library.rs 的 FileNode 加 abs_path**

在 library.rs 中，FileNode 结构与 get_package_files command 增加绝对路径。修改 FileNode 定义：

```rust
#[derive(Debug, Serialize)]
pub struct FileNode {
    pub id: i64,
    pub rel_path: String,
    pub name: String,
    pub ext: String,
    pub kind: String,
    pub bytes: i64,
    pub abs_path: String,   // 新增：完整路径，供前端 convertFileSrc
}
```

并修改 get_package_files，JOIN packages 取 path 拼绝对路径：

```rust
#[tauri::command]
pub async fn get_package_files(
    pkg_id: i64,
    pool: State<'_, SqlitePool>,
) -> Result<Vec<FileNode>, String> {
    let rows: Vec<(i64, String, String, String, String, i64, String)> = sqlx::query_as(
        "SELECT f.id,f.rel_path,f.name,f.ext,f.kind,f.bytes,p.path
         FROM files f JOIN packages p ON p.id=f.package_id
         WHERE f.package_id=? AND f.deleted=0 ORDER BY f.rel_path",
    )
    .bind(pkg_id)
    .fetch_all(&*pool)
    .await
    .map_err(|e| e.to_string())?;
    Ok(rows
        .into_iter()
        .map(|(id, rel, name, ext, kind, bytes, pkg_path)| FileNode {
            id, rel_path: rel, name, ext, kind, bytes,
            abs_path: format!("{}/{}", pkg_path, rel),
        })
        .collect())
}
```

- [ ] **Step 2: 同步 src/types/library.ts 的 FileNode 加 abs_path**

```ts
export interface FileNode {
  id: number;
  rel_path: string;
  name: string;
  ext: string;
  kind: string;
  bytes: number;
  abs_path: string;
}
```

- [ ] **Step 3: 创建 src/ipc/fileUrl.ts**

```ts
import { convertFileSrc } from "@tauri-apps/api/core";
import type { FileNode } from "../types/library";

// Windows 路径需转成 file URI；convertFileSrc 处理协议注入。
// 注意：abs_path 形如 "D:/Xiaoke/.../x.png"，convertFileSrc 期望原生路径。
export function getFileUrl(f: FileNode): string {
  return convertFileSrc(f.abs_path);
}
```

- [ ] **Step 4: 放开 fs 读权限（capabilities）**

修改 src-tauri/capabilities/default.json，给 opener 之外加 fs 与 asset 协议读权限：

```json
{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "core:event:default",
    {
      "identifier": "fs:allow-read-file",
      "allow": [{ "path": "**" }]
    }
  ]
}
```

> Tauri 2 的 convertFileSrc 依赖 `asset:` 协议，默认在 core:default 内。若图片加载不出来，参见 Step 6 排错。

- [ ] **Step 5: 验证编译**

Run: `cd src-tauri && cargo build`
Expected: 通过。

Run: `npm run build`
Expected: 通过。

- [ ] **Step 6: Commit**

```bash
git -C "D:/Xiaoke/Works/Tools/XiaokeTools" add -A
git -C "D:/Xiaoke/Works/Tools/XiaokeTools" commit -m "feat(m2): load image thumbnails via convertFileSrc"
```

> **排错备注（给执行者）**：若 convertFileSrc 图片不显示，常见原因：
> 1. capabilities 未放开 asset 协议——Tauri 2 默认 `core:default` 含 `core:asset:default`，一般够用。
> 2. 路径含中文——convertFileSrc 会做 encodeURI，通常 OK；若不行，检查 abs_path 的分隔符（统一用 `/`）。
> 3. 文件确实不存在（被软删除）——查询已过滤 deleted=0，不应出现。

---

## Task 8: PreviewPane 右栏 + 组装完整三栏

**Files:**
- Create: `src/components/PreviewPane.vue`
- Modify: `src/App.vue`（接入 PreviewPane）

- [ ] **Step 1: 创建 src/components/PreviewPane.vue**

```vue
<script setup lang="ts">
import { computed } from "vue";
import { storeToRefs } from "pinia";
import { useLibraryStore } from "../stores/libraryStore";
import { useSelectionStore } from "../stores/selectionStore";
import { getFileUrl } from "../ipc/fileUrl";

const lib = useLibraryStore();
const sel = useSelectionStore();
const { files } = storeToRefs(lib);
const { previewFileId } = storeToRefs(sel);

const file = computed(() => files.value.find((f) => f.id === previewFileId.value) ?? null);

function fmtBytes(b: number): string {
  if (b > 1e6) return (b / 1e6).toFixed(1) + " MB";
  if (b > 1e3) return (b / 1e3).toFixed(0) + " KB";
  return b + " B";
}
</script>

<template>
  <aside class="w-80 shrink-0 border-l border-slate-700 bg-slate-800/50 flex flex-col">
    <div class="px-3 py-2 text-sm text-slate-300 border-b border-slate-700">预览</div>
    <div v-if="file" class="flex-1 flex flex-col overflow-auto">
      <div class="flex-1 flex items-center justify-center bg-slate-900 min-h-48">
        <img v-if="file.kind === 'image'" :src="getFileUrl(file)" class="max-w-full max-h-96 object-contain" />
        <div v-else class="text-5xl">📦</div>
      </div>
      <div class="p-3 text-sm space-y-1">
        <div class="font-medium break-all">{{ file.name }}</div>
        <div class="text-slate-400">{{ fmtBytes(file.bytes) }} · {{ file.ext }} · {{ file.kind }}</div>
        <div class="text-slate-500 text-xs break-all">{{ file.rel_path }}</div>
      </div>
    </div>
    <div v-else class="flex-1 flex items-center justify-center text-slate-500 text-sm">
      点击文件预览
    </div>
  </aside>
</template>
```

- [ ] **Step 2: 更新 src/App.vue 接入 PreviewPane**

```vue
<script setup lang="ts">
import { onMounted } from "vue";
import TopBar from "./components/TopBar.vue";
import CategoryTree from "./components/CategoryTree.vue";
import PackageGrid from "./components/PackageGrid.vue";
import PreviewPane from "./components/PreviewPane.vue";
import { useLibraryStore } from "./stores/libraryStore";

const store = useLibraryStore();
onMounted(async () => {
  await store.loadLibraries();
  if (store.currentLibId !== null) await store.loadCategories();
});
</script>

<template>
  <div class="h-full flex flex-col bg-slate-900 text-slate-100">
    <TopBar />
    <div class="flex-1 flex overflow-hidden">
      <CategoryTree />
      <PackageGrid />
      <PreviewPane />
    </div>
  </div>
</template>
```

- [ ] **Step 3: 扩大窗口尺寸到 1280×800**

修改 src-tauri/tauri.conf.json 的 windows 段：

```json
"windows": [
  {
    "title": "XiaokeTools 资源管理器",
    "width": 1280,
    "height": 800,
    "minWidth": 960,
    "minHeight": 600
  }
]
```

- [ ] **Step 4: 验证编译**

Run: `npm run build`
Expected: 通过。

- [ ] **Step 5: Commit**

```bash
git -C "D:/Xiaoke/Works/Tools/XiaokeTools" add -A
git -C "D:/Xiaoke/Works/Tools/XiaokeTools" commit -m "feat(m2): PreviewPane and full three-pane layout"
```

---

## Task 9: M2 端到端验收

- [ ] **Step 1: 启动应用**

Run: `npm run tauri dev`
Expected: 1280×800 窗口，深色三栏界面。

- [ ] **Step 2: 添加库并扫描**

在应用内点「+ 添加库」，输入：
- 根目录：`D:\Xiaoke\GameAssets`
- 名称：`GameAssets`

点「扫描」，等待顶栏显示「上次扫描：42503 文件 / 5s」。

- [ ] **Step 3: 验收浏览闭环**

依次验证：
1. 左栏出现 12 个分类（01_平台跳跃 … 13_3D模型），各显示包数/文件数/体积
2. 点某个分类 → 中栏显示该分类的素材包网格
3. 点某个包（如 Platformer_Art_Complete_Pack）→ 中栏切换为缩略图墙
4. **图片缩略图正确显示**（PNG 棋盘格透明背景效果暂不要求，能显示即可）
5. 点一个图片文件 → 右栏预览大图 + 文件信息
6. 点「← 返回包列表」回到包网格

- [ ] **Step 4: 核对验收标准**

M2 验收点（来自设计 §7）：
- [x] 三栏布局正常
- [x] 点分类看包，点包看缩略图墙
- [x] 图片缩略图能显示

- [ ] **Step 5: Commit 验收**

```bash
git -C "D:/Xiaoke/Works/Tools/XiaokeTools" add -A
git -C "D:/Xiaoke/Works/Tools/XiaokeTools" commit -m "test(m2): e2e acceptance browse GameAssets"
```

---

## M2 完成定义

- [ ] Tailwind v4 + Pinia 接入，深色主题生效
- [ ] 三栏布局：分类树 / 包网格+文件缩略图墙 / 预览面板
- [ ] 添加库、扫描、浏览全流程可用
- [ ] 图片（image 类）缩略图在虚拟滚动墙中正确显示
- [ ] 点图片右栏预览大图
- [ ] 窗口 1280×800，三栏比例协调
